<div class="container">
  <div class="row">
    <h1>dsdsdsd</h1>
    <% @flats.each do |flat| %>
      <div class="col-sm-4">
          <div class="card">
            <h3><%= flat.name %></h3>
            <p><%= flat.address %></p>
            <p><%= flat.description %></p>
          </div>
      </div>
    <% end %>
  </div>
</div>
